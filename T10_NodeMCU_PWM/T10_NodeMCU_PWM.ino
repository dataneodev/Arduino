void setup() {
  // put your setup code here, to run once:
  pinMode(D0, OUTPUT);
  analogWrite(D0, 0);
  
  pinMode(D1, OUTPUT);
  analogWrite(D1, 102);

  pinMode(D2, OUTPUT);
  analogWrite(D2, 204);

  pinMode(D3, OUTPUT);
  analogWrite(D3, 306);

  pinMode(D4, OUTPUT);
  analogWrite(D4, 408);

  pinMode(D5, OUTPUT);
  analogWrite(D5, 510);

  pinMode(D6, OUTPUT);
  analogWrite(D6, 612);

  pinMode(D7, OUTPUT);
  analogWrite(D7, 714);

  pinMode(D8, OUTPUT);
  analogWrite(D8, 816);
}

void loop() {
  // put your main code here, to run repeatedly:

}
